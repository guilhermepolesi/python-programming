# create the variables: john, mary and adam
# assign values to variables. The values must be equal to the amount of fruit owned by
# John, Mary and Adam, respectively
# having stored the numbers in the variables, print the variables on one line and separate each of them with a comma
# Now create a new variable called total_apples equal to the addition of the previous three variables.
# print the value stored in total_apples to the console

john = 3
mary = 5
adam = 6
print(john, mary, adam, sep=",")

total_apples = john + mary + adam
print("Total number of apples:", total_apples)